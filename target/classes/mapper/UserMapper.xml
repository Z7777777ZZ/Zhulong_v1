<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zsj.demo_v1.mapper.UserMapper">
    <resultMap id="userResultMap" type="org.zsj.demo_v1.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="avatar" column="avatar"/>
        <result property="status" column="status"/>
        <result property="role" column="role"/>
        <result property="registerTime" column="register_time"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="lastLoginIp" column="last_login_ip"/>
    </resultMap>
    
    <sql id="userColumns">
        id, username, password, email, phone, avatar, status, role, register_time, last_login_time, last_login_ip
    </sql>
    
    <select id="findByUsername" resultMap="userResultMap">
        SELECT <include refid="userColumns"/>
        FROM user
        WHERE username = #{username}
    </select>
    
    <select id="findByEmail" resultMap="userResultMap">
        SELECT <include refid="userColumns"/>
        FROM user
        WHERE email = #{email}
    </select>
    
    <select id="findById" resultMap="userResultMap">
        SELECT <include refid="userColumns"/>
        FROM user
        WHERE id = #{id}
    </select>
    
    <insert id="insert" parameterType="org.zsj.demo_v1.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (
            username, password, email, phone, avatar, status, role, register_time, last_login_time, last_login_ip
        ) VALUES (
            #{username}, #{password}, #{email}, #{phone}, #{avatar}, #{status}, #{role}, #{registerTime}, #{lastLoginTime}, #{lastLoginIp}
        )
    </insert>
    
    <update id="update" parameterType="org.zsj.demo_v1.entity.User">
        UPDATE user
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="status != null">status = #{status},</if>
            <if test="role != null">role = #{role},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
            <if test="lastLoginIp != null">last_login_ip = #{lastLoginIp},</if>
        </set>
        WHERE id = #{id}
    </update>
    
    <update id="updateLoginInfo">
        UPDATE user
        SET last_login_time = NOW(), last_login_ip = #{lastLoginIp}
        WHERE id = #{id}
    </update>
    
    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM user
        WHERE username = #{username}
    </select>
    
    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM user
        WHERE email = #{email}
    </select>
</mapper> 